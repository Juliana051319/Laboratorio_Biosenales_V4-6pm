# Laboratorio_Biose-ales

## Flujo de trabajo colaborativo en Git

Para la entrega de las prácticas del curso se sigue el siguiente flujo de trabajo:

* **[ Git Pull ]** --> Actualizar el repositorio local desde GitHub
* **[ Desarrollo ]** --> Resolver práctica en Google Colab / VS Code
* **[ Git Add ]** --> Preparar los archivos modificados (`git add .`)
* **[ Git Commit ]** --> Guardar estado local con mensaje (`git commit -m "..."`)
* **[ Git Push ]** --> Subir los cambios al repositorio remoto en GitHub

### Comandos principales utilizados:
1. **Traer cambios:** `git pull origin main`
2. **Agregar archivos:** `git add .`
3. **Guardar cambios:** `git commit -m "Entrega Practica 01"`
4. **Subir cambios:** `git push origin main`
